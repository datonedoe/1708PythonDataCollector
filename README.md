# 1708PythonDataCollector

* Some data has been removed from the app.py and send_email.py due to security risks. The app can run fine if you clone the repo and add your own credentials to those files.
